[package]
authors = ["{{author}} <{{email}}>"]

name = "{{project_name_snake}}-rbac"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "workspace = true"
casbin = { version = "2.5.0", features = ["tokio"] }
thiserror = "2.0"
tokio = { version = "workspace = true", features = ["full"] }
serde = { workspace = true }

[dev-dependencies]
tokio = { version = "workspace = true", features = ["full", "test-util"] }
