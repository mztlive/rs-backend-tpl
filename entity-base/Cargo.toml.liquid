[package]
authors = ["{{author}} <{{email}}>"]

name = "entity-base"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true, features = ["derive"] }
chrono = { workspace = true }
