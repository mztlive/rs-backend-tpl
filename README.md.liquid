# {{project_name_pascal}}

{{project_description}}

## 快速开始

### 开发环境

1. 安装依赖:   ```bash
   cargo build   ```

2. 运行服务:   ```bash
   cargo run -p api   ```

### Docker部署

1. 构建镜像:   ```bash
   docker build -t {{project_name_snake}} .   ```

2. 运行容器:   ```bash
   docker run -d \
     -p 10001:10001 \
     -v /path/to/config.toml:/app/config.toml \
     -v /path/to/uploads:/uploads \
     {{project_name_snake}}   ```

## 项目结构

- `api`: Web API服务
- `task`: 后台任务服务
- `entity-base`: 实体基础库
- `entity-derive`: 实体派生宏
- `rbac`: 权限控制
- `database`: 数据库操作
- `entities`: 数据实体
- `services`: 业务服务
- `storage`: 存储服务
- `libs`: 通用库
- `config`: 配置管理
- `container`: 依赖注入容器

## 作者

{{author}} <{{email}}> 